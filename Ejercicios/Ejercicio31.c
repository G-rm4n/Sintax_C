#include <stdio.h>

int main( ) {
    int a;
    scanf("%d", &a);
    if (a>=0) {
        printf("El numero es positivo\n");
    } else {
        printf("El numero es negativo\n");
    };
    return 0;
}